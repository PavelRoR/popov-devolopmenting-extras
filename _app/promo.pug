-var path = '../';
-var head_url = 'https://popov.mastervision.su/developmenting-extras/promo/';
-var head_title = 'Вы зарегистрированы на мастер-класс «Введение в эзотерику. Путешествие души»';
-var head_description = 'Подпишитесь в Telegram';
-var styles = path+'css/promo.min.css';
-var fav_ico = path+'favicon.ico';
-var fav_png = path+'favicon.png';
-var noref = true;
-var ak = true;
-var footer_title = true;
//- -var oferta = 'https://mastervision.su/oferta.docx';

mixin b(cl, clb, href, p, target, id)
	.button-wrapper(class=cl)
		a.button(class=clb href!=href target=target id=id)=p
mixin gt(cl, h3)
	h3.general-title(class=cl)=h3
mixin imgs(cl, src ,alt)
	img(class=cl src=path+src alt=alt)
mixin gul(cl, ...items)
	ul.general-list(class='general-list-'+cl)
		each item in items
			li=item

<!DOCTYPE html>
html(lang="ru")
	head
		include pug/head
		include pug/scripts/head_scripts
	body
		include pug/scripts/body_top_scripts
		.wrapper
			include pug/base/1-header
			include pug/promo/1-main
		include pug/base/2-footer
		include pug/scripts/body_bottom_scripts